<configuration>
    <appender name="USER_MESSAGES" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/messages.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeBasedRollingPolicy">
            <maxFileSize>10MB</maxFileSize>
            <fileNamePattern>logs/messages-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="ERRORS_WARNINGS" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/errors.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeBasedRollingPolicy">
            <maxFileSize>10MB</maxFileSize>
            <fileNamePattern>logs/errors-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="user.messages" level="INFO" additivity="false">
        <appender-ref ref="USER_MESSAGES" />
    </logger>

    <logger name="server.errors" level="WARN" additivity="false">
        <appender-ref ref="ERRORS_WARNINGS" />
    </logger>

    <root level="INFO">
        <appender-ref ref="USER_MESSAGES" />
        <appender-ref ref="ERRORS_WARNINGS" />
    </root>
</configuration>